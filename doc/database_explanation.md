# شرح مخطط قاعدة البيانات - ERP شركة رصاص للبلاستيك

يوفر هذا المستند نظرة شاملة على جداول قاعدة البيانات المحددة في [sss_mysql.sql](file:///d:/2026/Rasras-Plastics/doc/sss_mysql.sql). تم تصميم النظام كنظام تخطيط موارد مؤسسات (ERP) متكامل لشركة تجارة بلاستيك، يغطي المشتريات، المبيعات، المخازن، المالية، الجودة، والموارد البشرية.

---

## 🏗️ الوحدات الأساسية وقاموس الجداول

### 1. دورة المشتريات (Purchase Cycle)
تدير هذه الوحدة دورة حياة شراء المواد الخام (مثل حبيبات البلاستيك HDPE, LDPE, PP) من الموردين.

| الجدول | الوصف والهدف |
| :--- | :--- |
| `suppliers` | البيانات الأساسية للموردين (محليين ودوليين). |
| `purchaserequisitions` | **طلب شراء داخلي (PR):** يصدر من المبيعات أو المخازن عند الاحتياج. |
| `requestforquotations` | **عروض أسعار (RFQ):** إرسال استفسارات الأسعار للموردين. |
| `supplierquotations` | تسجيل عروض الأسعار الواردة من الموردين. |
| `quotationcomparisons` | **مقارنة عروض (QCS):** تحليل العروض واختيار الأنسب اعتماداً على السعر والجودة. |
| `purchaseorders` | **أمر شراء رسمي (PO):** العقد الرسمي مع المورد. |
| `goodsreceiptnotes` | **إذن استلام (GRN):** إثبات وصول البضاعة للمخزن مبدئياً. |
| `supplierinvoices` | فاتورة المورد لتسجيل الاستحقاق المالي. |
| `purchasereturns` | مرتجعات المشتريات للمواد المرفوضة أو التالفة. |
| `debitnotes` | إشعارات الخصم لتعديل أرصدة الموردين. |

---

### 2. دورة المبيعات (Sales Cycle)
تختص بإدارة علاقات العملاء ودورة طلبات البيع حتى التحصيل.

| الجدول | الوصف والهدف |
| :--- | :--- |
| `customers` | البيانات الأساسية للعملاء، حدود الائتمان، وفئات التسعير. |
| `salesquotations` | عروض الأسعار المرسلة للعملاء. |
| `salesorders` | **أمر بيع (SO):** الطلبات المعتمدة الجاهزة للتنفيذ. |
| `stockissuenotes` | **إذن صرف:** تسجيل خروج البضاعة من المخزن فعلياً. |
| `deliveryorders` | أوامر التوصيل ومتابعة اللوجستيات (المركبة، السائق، المنطقة). |
| `salesinvoices` | فواتير المبيعات (تدعم متطلبات الفاتورة الإلكترونية). |
| `salesreturns` | مرتجعات المبيعات من العملاء. |
| `creditnotes` | إشعارات إضافة لتعديل حساب العميل (في حال المرتجعات). |
| `pricelists` | لوائح الأسعار المعتمدة (جملة، قطاعي، موزع). |

---

### 3. إدارة المخازن والمخزون (Inventory & Warehouse)
متابعة كميات المخزون، الحركات، والجرد.

| الجدول | الوصف والهدف |
| :--- | :--- |
| `items` | بيانات الأصناف (مواد خام، إضافات، منتجات تامة). |
| `warehouses` | المستودعات (مخزن الخام، مخزن المنتج التام). |
| `stockbalances` | رصيد المخزون الحالي لكل صنف في كل موقع. |
| `stockmovements` | سجل حركات المخزون (إضافة، صرف، تحويل) للرقابة والتدقيق. |
| `inventorybatches` | تتبع المخزون بنظام "التشغيلة" (Batch) لدعم نظام FIFO وتتبع الجودة. |
| `stocktransfers` | التحويلات المخزنية بين فروع ومستودعات الشركة. |
| `stockadjustments` | تسويات المخزون الناتجة عن عمليات الجرد الفعلي. |

---

### 4. النظام المالي والمحاسبي (Finance & Accounting)
القلب النابض للنظام لإدارة الحسابات والمدفوعات.

| الجدول | الوصف والهدف |
| :--- | :--- |
| `chartofaccounts` | **دليل الحسابات:** شجرة الحسابات (أصول، خصوم، إيرادات، مصروفات). |
| `journalentries` | **قيود اليومية:** السجل الأساسي لكل العمليات المالية. |
| `cashregisters` | إدارة الخزن النقدية (العهدة، النثرية). |
| `bankaccounts` | الحسابات البنكية ومتابعة أرصدتها. |
| `receiptvouchers` | **سندات قبض:** تسجيل المبالغ المستلمة من العملاء. |
| `paymentvouchers` | **سندات صرف:** تسجيل المدفوعات للموردين أو المصروفات العمومية. |
| `chequesreceived` | الشيكات المستلمة ومتابعة حالاتها (تحت التحصيل، مرتد، محصل). |
| `exchangerates` | أسعار الصرف لدعم العملات الأجنبية (USD, EUR). |

---

### 5. الرقابة على الجودة (Quality Control)
ضمان مطابقة المواد الخام والمنتجات للمواصفات الفنية.

| الجدول | الوصف والهدف |
| :--- | :--- |
| `qualityparameters` | تعريف المعايير (اللزوجة، معامل التدفق، اللون، الرطوبة). |
| `itemqualityspecs` | المواصفات القياسية المقبولة لكل صنف. |
| `qualityinspections` | سجل عمليات الفحص الفعلي ونتائج القبول/الرفض. |

---

### 6. الموارد البشرية والصلاحيات (HR & Security)

| الجدول | الوصف والهدف |
| :--- | :--- |
| `employees` | ملفات الموظفين، الإدارات، والبيانات الوظيفية. |
| `users` | حسابات دخول النظام المرتبطة بالموظفين. |
| `roles` / `permissions` | نظام الصلاحيات (مدير، محاسب، أمين مخزن). |
| `attendance` | سجل الحضور والانصراف. |
| `payroll` | مسيرات الرواتب، البدلات، والاستقطاعات. |

---

## 🔄 تكامل الدورات المستندية (Document Integration)

يربط النظام المستندات ببعضها لضمان الرقابة (عبر جدول `documentrelationships`):

1.  **في المشتريات:** لا يمكن إصدار `PO` بدون `PR` معتمد، ولا يمكن الاستلام `GRN` إلا بموجب `PO`.
2.  **في المبيعات:** يمنع صرف أي مادة من المخزن `Stock Issue` بدون وجود أمر بيع `SO` صحيح.
3.  **في المالية:** يتم توليد قيود اليومية `Journal Entries` تلقائياً فور اعتماد الفواتير أو السندات.
4.  **في الجودة:** لا يتم تحويل حالة البضاعة في المخزن من "قيد الفحص" إلى "متاح" إلا بعد صدور تقرير قبول من الجودة.

---

## 📖 قاموس الحقول التفصيلي (Detailed Field Dictionary)

فيما يلي شرح لأهم الحقول في الجداول الرئيسية لسهولة الفهم التقني والوظيفي:

### أصناف المخزون (Items)
- `ItemID`: المعرف الرقمي الفريد للصنف.
- `ItemCode`: رمز الصنف (كود المنتج).
- `ItemNameAr / ItemNameEn`: اسم الصنف بالعربية والإنجليزية.
- `CategoryID`: ربط الصنف بالفئة (مثلاً: بولي إيثيلين، بولي بروبيلين).
- `UnitID`: وحدة القياس الأساسية (كجم، طن، كيس).
- `ReorderLevel`: حد إعادة الطلب (تنبيه عند انخفاض المخزون).

### عروض الأسعار والمشتريات (Procurement)
- `TargetValue` (في `itemqualityspecs`): القيمة المستهدفة للاختبارات الفنية.
- `IsCritical`: علامة توضح إذا كان هذا المعيار حرجاً للجودة.
- `UnitPrice`: سعر الوحدة المتفق عليه مع المورد أو العميل.
- `TaxAmount`: قيمة الضريبة المحتسبة على السطر.

### القيود والحسابات (Accounting)
- `AccountNature`: طبيعة الحساب (مدين أو دائن).
- `IsControlAccount`: يحدد ما إذا كان حساباً رقابياً (لا يقبل القيود اليدوية المباشرة).
- `Debit / Credit`: إجمالي مبالغ القيد (الجانب المدين والدائن).
- `PostingStatus`: حالة الترحيل (مسودة، مرحل، ملغى).

---

## 📊 التقارير الرئيسية (Views)

توفر قاعدة البيانات رؤى برمجية جاهزة للتقارير:
- `vw_stocksummary`: ملخص المخزون الفعلي والقيمة المالية.
- `vw_customeroutstanding`: أعمار الديون وكشوف حسابات العملاء.
- `vw_supplieroutstanding`: مستحقات الموردين وجدولة الدفع.
- `vw_pendingapprovals`: قائمة مركزية لطلبات الاعتماد المعلقة للمديرين.

---
> [!TIP]
> جميع الجداول تحتوي على حقول التدقيق (`CreatedAt`, `CreatedBy`, `UpdatedAt`, `UpdatedBy`) لضمان تتبع من قام بكل عملية وتاريخها. كما تدعم الجداول الأسماء باللغتين العربية والإنجليزية.
